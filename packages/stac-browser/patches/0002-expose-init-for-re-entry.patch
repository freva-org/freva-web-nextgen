diff --git a/src/main.js b/src/main.js
index 284b4ab..2ff3ff4 100644
--- a/src/main.js
+++ b/src/main.js
@@ -1,3 +1,8 @@
 import init from "./init";
 
+// Freva embed: an ES module is evaluated once per document, so the bare init()
+// below runs only on the first visit. The portal calls this on every later
+// entry to the STAC route.
+window.STAC_BROWSER_INIT = init;
+
 init();
