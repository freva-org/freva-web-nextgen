/** Types for `py-json.mjs`, for the unit test that holds it. */
export declare function pythonJsonExpression(expression: string): string;
export declare function jsonFromPythonRepr(repr: string): unknown;
