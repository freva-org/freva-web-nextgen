This portal is a fixture. Every page it serves is a real file produced by the
portal builder, and nothing about its structure is fetched at runtime.

Prose blocks like this one are ordinary Markdown sources that own no route of
their own: they are rendered into the page that references them.
